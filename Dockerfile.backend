# Dockerfile para backend API con PostgreSQL
FROM node:18-alpine

# Establecer directorio de trabajo
WORKDIR /app

# Copiar archivos de dependencias
COPY package*.json ./

# Instalar dependencias de producción (incluyendo pg para PostgreSQL)
RUN npm ci --only=production

# Instalar pg específicamente
RUN npm install pg

# Copiar servidor backend corregido
COPY backend-server-fixed.cjs ./backend-server.cjs

# Exponer puerto 8080
EXPOSE 8080

# Variables de entorno
ENV NODE_ENV=production

# Comando para iniciar el servidor backend
CMD ["node", "backend-server.cjs"] 