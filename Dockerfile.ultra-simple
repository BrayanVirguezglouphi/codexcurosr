FROM node:18-alpine

WORKDIR /app

# Solo copiar el servidor simple - SIN dependencias externas
COPY simple-server.js ./

# Exponer puerto
EXPOSE 8080
ENV PORT=8080

# Comando simple
CMD ["node", "simple-server.js"] 