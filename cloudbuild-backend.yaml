steps:
  # Build backend image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-f', 'Dockerfile.backend', '-t', 'gcr.io/$PROJECT_ID/pros-backend:$BUILD_ID', '.']
  
  # Push backend image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/pros-backend:$BUILD_ID']
  
  # Deploy to Cloud Run
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'pros-backend'
      - '--image'
      - 'gcr.io/$PROJECT_ID/pros-backend:$BUILD_ID'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '8080'
      - '--set-env-vars'
      - 'NODE_ENV=production,DB_HOST=35.238.111.59,DB_PORT=5432,DB_NAME=SQL_DDL_ADMCOT,DB_USER=postgres,DB_PASSWORD=123456789'

options:
  logging: CLOUD_LOGGING_ONLY 